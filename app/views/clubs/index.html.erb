<h1>Clubs</h1>

<% @clubs.each do |club| %>
	<p><%= club.club_name %></p>
	<p><%= club.address %></p>
	<p><%= club.sport %></p>
	<%= image_tag club.main_image unless club.main_image.nil? %>
	<p><%= link_to "More info", club_show_path(club.id) %></p>
	<br>
<% end %>